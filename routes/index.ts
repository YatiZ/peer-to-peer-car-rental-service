export const AUTH_ROUTES = [
  "/auth?tab=login",
  "/auth?tab=register",
];

export const PUBLIC_ROUTES = ["/", "/about", "/fleet"];

export const GUARD_ROUTES = ["/dashboard", "/profile"];

export const DEFAULT_LOGIN_REDIRECT = "/";
